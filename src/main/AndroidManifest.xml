<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ansengas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnsenGas">
        <activity android:name=".Ui.customer_index"></activity>
        <activity android:name=".InvoiceDetailActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".Ui.MainActivity" />
        <activity android:name=".Ui.InvoiceActivity" />
        <activity android:name=".Ui.ViewOldInvoiceActivity" />
        <activity android:name=".Ui.ViewCustomersActivity" />
        <activity android:name=".Ui.HomeActivity" />
        <activity android:name=".Ui.invoice_payment" />
        <activity android:name=".Ui.splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>